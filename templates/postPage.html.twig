{% extends "layout.html.twig" %}

{% block content %}
    {% for post in posts %}
        <a href="/articles/{{ post.postId }}"><h1>{{ post.title }}</h1></a>
        <p>{{ post.content }}</p>
    {% endfor %}
{% endblock %}