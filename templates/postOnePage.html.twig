{% extends "layout.html.twig" %}

{% block content %}

        <h1>{{ post.title }}</h1>
        <p>{{ post.content }}</p>
        <h1>{{ post.id }}</h1>

        
        <p>{{ comments.content }}</p>
        <h1>{{ comments.id }}</h1>

{% endblock %}