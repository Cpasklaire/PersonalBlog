{% extends "layout.html.twig" %}

{% block content %}

        <h1>{{ post.title }}</h1>
        <p>{{ post.content }}</p>
        <h1>{{ post.id }}</h1>

        
        <p>{{ comment.content }}</p>
        <h1>{{ comment.id }}</h1>

{% endblock %}